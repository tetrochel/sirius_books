name: sirius_books
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.8.1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  presentation:
    path: ./presentation
  surf_lint_rules: ^1.3.0

flutter:
  uses-material-design: true
